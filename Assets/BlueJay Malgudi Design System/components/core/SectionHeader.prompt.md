Serif page/section header — the top of every BlueJay Malgudi screen.

```jsx
<SectionHeader eyebrow="Good morning," title="Spotlight"
  subtitle="Your community at a glance" />
```

Optional `eyebrow` (uppercase label), `subtitle`, and a signature short rule (`rule`, on by default). `align="center"` for hero/empty states.
